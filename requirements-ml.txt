# ML training dependencies — kept separate from runtime requirements.txt
# so Lambda packages don't ship a 600MB torch wheel.
#
# Install only on the training/build machine:
#   pip install -r requirements-ml.txt
torch>=2.2.0
onnx>=1.15.0
onnxruntime>=1.17.0
scikit-learn>=1.4.0
numpy>=1.26.0
